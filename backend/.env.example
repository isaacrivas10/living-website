MODEL_BASE_URL=http://localhost:9000/v1
MODEL_API_KEY=local
MODEL_NAME=Qwen3.6-35B-A3B
REQUEST_TIMEOUT_SECONDS=120
FRONTEND_ORIGIN=http://localhost:5173
MODEL_TEMPERATURE=0.4
MODEL_MAX_TOKENS=16384
# Disable Qwen3 chain-of-thought for generation tasks (set true for oMLX / servers
# that don't support enable_thinking and might error on unknown extra_body params).
ENABLE_THINKING=false
# Override cache directory (default: repo-root/data/cache). Set for Docker deployments.
# CACHE_DIR=/data/cache
